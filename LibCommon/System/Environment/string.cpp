#include <System/Environment/String.h>
// This translation unit intentionally left blank.
// All template member implementations for `Common::System::Environment::String`
// are defined inline in the header `String.h`. Defining them again here
// causes duplicate-definition errors. Keep this file as a header-only stub.

// This translation unit intentionally left blank.
//
// All template member definitions for `Common::System::Environment::String`
// are provided inline in the header (`String.h`). Defining them again here
// causes duplicate definition errors when the header is included in multiple
// translation units. Keep this file empty (or use it only for explicit
// instantiations if you need them).